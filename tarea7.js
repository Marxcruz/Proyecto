//Convertir nombres a mayúsculas:
//Dado un array de nombres, usa map() para convertirlos todos a mayúsculas.

const convertirAMayusculas = (nombres) => nombres.map(nombre => nombre.toUpperCase());
console.log(convertirAMayusculas(["ana", "juan", "luis"])); 
